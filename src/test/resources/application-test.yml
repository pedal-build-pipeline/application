spring:
  jpa:
    show-sql: true

features:
  toggles:
    users:
      registration-enabled: true

outbox:
  polling:
    enabled: false

notifications:
  email:
    addresses:
      from:
        name: PBP
        address: pbp@test.com
      replyto:
        name: "No Reply"
        address: no-reply@test.com